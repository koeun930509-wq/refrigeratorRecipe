# PRD - Step 2: 레시피 생성

## 개요
1단계에서 얻은 재료 목록(사용자가 수정한 최종 목록 포함)을 바탕으로, 텍스트 생성 모델을 이용해 실제로 만들 수 있는 레시피를 추천한다.

## 목표
- 보유 재료만으로 만들 수 있는(또는 최소한의 추가 구매로 만들 수 있는) 레시피를 3개 내외로 제안한다.
- 각 레시피는 재료, 조리 순서, 예상 조리 시간을 포함한다.

## 사용 모델
- `openai/gpt-oss-20b:free` (OpenRouter)
- **주의**: `:free` 접미사를 반드시 붙일 것. 접미사 없는 `openai/gpt-oss-20b`는 유료 모델이며, 실제 테스트에서 `402 Insufficient credits` 에러가 확인됨. 접미사가 붙은 `:free` 버전은 정상 동작 확인됨.

## 사용자 흐름
1. 사용자가 1단계 결과(재료 목록)를 확인/수정한 뒤 "레시피 추천받기" 버튼을 누른다.
2. (선택) 인분 수, 조리 시간, 알레르기/비선호 재료 등 간단한 옵션을 입력한다.
3. 서버가 재료 목록과 옵션을 프롬프트로 구성하여 OpenRouter API를 호출한다.
4. 모델이 생성한 레시피 목록(제목, 재료, 조리법, 예상 시간)을 화면에 카드 형태로 표시한다.
5. 사용자는 마음에 드는 레시피를 선택해 상세 조리법을 확인할 수 있다.

## API 명세

### `POST /api/generate-recipes`

**Request**
```json
{
  "ingredients": ["계란", "우유", "대파", "두부"],
  "options": {
    "servings": 2,
    "max_cook_time_minutes": 30,
    "exclude": ["매운 음식"]
  }
}
```
- `options`는 선택 항목이며 생략 가능.

**Processing**
1. 재료 목록과 옵션을 조합해 프롬프트 생성.
   - 예시: "다음 재료를 활용한 레시피 3개를 추천해줘: 계란, 우유, 대파, 두부. 각 레시피는 제목, 필요 재료(보유 재료 외 추가로 필요한 것 표시), 조리 순서(단계별), 예상 조리 시간을 포함해서 JSON으로만 응답해줘."
2. OpenRouter Chat Completions API 호출 (`openai/gpt-oss-20b:free`).
3. 응답을 JSON으로 파싱. 실패 시 1회 재시도.

**Response (성공, 200)**
```json
{
  "recipes": [
    {
      "title": "대파 두부 계란찜",
      "used_ingredients": ["계란", "대파", "두부"],
      "additional_ingredients": ["소금", "참기름"],
      "steps": [
        "두부를 잘게 으깬다.",
        "계란과 대파를 섞는다.",
        "찜기에 넣고 15분간 찐다."
      ],
      "estimated_time_minutes": 20
    }
  ]
}
```

**Response (실패)**
- 400: 재료 목록 누락(빈 배열)
- 422: 모델 응답 JSON 파싱 실패
- 502: OpenRouter API 호출 실패 (크레딧 부족 포함, 원문 에러 메시지 전달)

## 비기능 요구사항
- 재료 목록이 1개 이하일 경우에도 최소한의 레시피(또는 "재료가 부족합니다" 안내)를 제공
- 응답 시간: 평균 15초 이내 목표

## 범위 제외 (Out of Scope)
- 영양 성분 계산
- 레시피 이미지 생성
- 다국어 레시피 지원 (한글 우선)

## 성공 기준
- 임의의 재료 조합 10개 테스트 시, 매번 최소 1개 이상의 실행 가능한 레시피 생성
- 모델이 유료 모델로 잘못 호출되어 크레딧 에러가 발생하지 않도록 모델명에 `:free` 명시 (회귀 테스트 항목으로 포함)


## 기술스택
phython, flask