# PRD - Step 1: 냉장고 사진 재료 인식

## 개요
사용자가 업로드한 냉장고(또는 식재료) 사진을 OpenRouter의 이미지 인식 모델에 전달하여, 사진 속에 보이는 식재료 목록을 구조화된 데이터로 추출한다.

## 목표
- 사용자가 사진 한 장을 업로드하면, 별도 입력 없이 "재료 후보 리스트"를 얻는다.
- 이 결과는 2단계(레시피 생성)의 입력으로 그대로 사용될 수 있어야 한다.

## 사용 모델
- `google/gemma-4-26b-a4b-it:free` (OpenRouter)
- 참고: 이미지 인식 모델은 URL 방식보다 base64 데이터 URI(`data:image/...;base64,...`)로 전달했을 때 안정적으로 동작함을 사전 테스트로 확인함. 외부 이미지 URL을 그대로 넘기면 provider(Google AI Studio)가 fetch에 실패하는 경우가 있었음.

## 사용자 흐름
1. 사용자가 웹 페이지에서 냉장고 사진을 업로드(파일 선택 또는 드래그 앤 드롭)한다.
2. 프론트엔드가 이미지를 서버로 전송한다.
3. 서버가 이미지를 base64로 인코딩하여 OpenRouter API에 전달한다.
4. 모델이 인식한 재료 목록을 JSON 형태로 반환받아 화면에 표시한다.
5. 사용자는 인식된 재료 목록을 확인하고, 필요 시 수동으로 항목을 추가/삭제할 수 있다.

## API 명세

### `POST /api/recognize-ingredients`

**Request**
- `multipart/form-data`
  - `image`: 이미지 파일 (jpg, png)

**Processing**
1. 업로드된 이미지를 base64로 인코딩.
2. OpenRouter Chat Completions API 호출:
   - model: `google/gemma-4-26b-a4b-it:free`
   - content: [텍스트 프롬프트, image_url(base64 data URI)]
   - 프롬프트 예시: "이 이미지는 냉장고 내부 사진이야. 보이는 식재료 이름만 한글로 JSON 배열로 알려줘. 형식: {\"ingredients\": [\"계란\", \"우유\", ...]}. 다른 설명은 붙이지 마."
3. 모델 응답에서 JSON 파싱 시도.
   - 파싱 실패 시, 재시도 1회(프롬프트에 "JSON만 출력" 재강조) 후에도 실패하면 에러 반환.

**Response (성공, 200)**
```json
{
  "ingredients": ["계란", "우유", "대파", "두부"]
}
```

**Response (실패)**
- 400: 이미지 파일 누락 또는 형식 오류
- 422: 모델 응답을 JSON으로 파싱하지 못함
- 502: OpenRouter API 호출 실패(모델 오류, 크레딧 부족 등 원문 메시지 포함)

## 비기능 요구사항
- 이미지 업로드 최대 용량: 5MB (초과 시 클라이언트에서 리사이즈 후 전송 권장)
- 응답 시간: 평균 10초 이내 목표 (무료 모델 특성상 지연 가능성 있음을 UI에 로딩 상태로 안내)

## 범위 제외 (Out of Scope)
- 재료의 수량/신선도 추정
- 여러 장의 사진 동시 업로드
- 바코드/포장 텍스트 OCR

## 성공 기준
- 냉장고 사진 10장 샘플 테스트 시, 육안으로 확인 가능한 주요 재료의 70% 이상을 정확히 인식
- API 에러 발생 시 사용자에게 명확한 안내 메시지 노출


## 기술스택
phython, flask